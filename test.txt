int x = 0;
int y = 23;

print((x + y) / 2 * 2);

///*
int x = 0;
int y = 0;
int a[100][100][100][100];
int b[100];
double m = 0.0;

int sum(int x, int y)
{
    int a;
    {
        {
            double b;
        }
        string c;
    }
    return x + y;
}
int sum(int x, int z)
{
    return x + z;
}
int sum(int x, double z)
{
    return x + z;
}
void out(int x)
{
    print(x);
}

f;
f();
x.f;
x.f();

sum(x,x);
sum(x,m);
sum(m,x);

b[6 * 2 - 4] = 2 + y;

if(x > y)
    if(x < 23)
    {
        x = 11;
        m = x + y / 2.0;
    }
    else
        x = 12;

while(x > 0)
{
    --x;
}

do {x++;} while(x < 1);
do  y++;  while(y < 13);

for(; x <= y; ++x)
    m += x;

class MyClass
{
    private double avg;
    public int x;
    public int y;

    private void update()
    {
        this.avg = (this.x + this.y) / 2;
    }
    public double getAvg()
    {
        this.update();
        return avg;
    }
}

MyClass c;
c.x = 1;
c.y = 2;
print(c.getAvg());
int y;


// This is a comment

/*Multi-line
* / comment
the next comment is empty
*/ /**/ /*/this comment is
not
closed
